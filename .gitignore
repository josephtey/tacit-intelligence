# Dataset and model artifacts (kept on /large_storage via symlinks)
data/
*.npy
*.pt
*.bin
*.safetensors

# Python
__pycache__/
*.py[cod]
*.so
*.egg
*.egg-info/
build/
dist/
.venv/
venv/
env/
.ipynb_checkpoints/

# Test / type-check / lint caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/

# Logs and caches
*.log
.cache/

# OS
.DS_Store
Thumbs.db

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Secrets — keys never enter version control
.env
.env.local

# Eval run outputs — keep small result artifacts in git, ignore heavy/derived ones.
# (runs/predictions, runs/scores, runs/manifests, runs/agent_traces, runs/report.md ARE tracked.)
# Extracted JPEG frames (~170 MB, regenerable from videos):
runs/frames/
runs/slurm_logs/
runs/lsv_download.log
